# ============================================================
# Finders Keepers — production environment
# Copy this file to ".env" on the server and fill in REAL values.
# NEVER commit the real .env (it is gitignored).
# ============================================================

# ---- Domains (point these DNS A-records at your server IP) ----
WEB_DOMAIN=finderskeeperslb.com
ADMIN_DOMAIN=admin.finderskeeperslb.com
API_DOMAIN=api.finderskeeperslb.com
ACME_EMAIL=charbel_makhlouf76@live.com
STORE_NAME=Finders Keepers

# ---- Postgres (self-hosted in the stack) ----
POSTGRES_USER=fkadmin
POSTGRES_PASSWORD=CHANGE_ME_strong_db_password
POSTGRES_DB=finders_keepers

# ---- JWT secrets — generate each with: openssl rand -hex 32 ----
JWT_SECRET=CHANGE_ME
JWT_REFRESH_SECRET=CHANGE_ME
CUSTOMER_JWT_SECRET=CHANGE_ME
CUSTOMER_JWT_REFRESH_SECRET=CHANGE_ME

# ---- Uploaded media: MinIO object storage (self-hosted in the stack) ----
# Uploads go to the MinIO "uploads" bucket and are served read-only at
# https://<API_DOMAIN>/uploads/... through Caddy.
#
# MINIO_ROOT_PASSWORD is a real credential: it grants full read/write on all
# media. Generate it, do not reuse another password:
#   openssl rand -hex 24
MINIO_ROOT_USER=fkminio
MINIO_ROOT_PASSWORD=CHANGE_ME_strong_minio_password
# Must stay "uploads": MinIO serves objects at /<bucket>/<key>, and that path is
# what every image URL in the database already points at. Renaming this breaks
# every image on the site.
MINIO_BUCKET=uploads
PUBLIC_MEDIA_BASE_URL=https://api.finderskeeperslb.com

# Read-only fallback for images uploaded before MinIO (StorageType.LOCAL).
# Leave as-is; nothing new is written here.
UPLOADS_DIR=/app/uploads

# ---- Mail (Hostinger SMTP) ----
MAIL_HOST=smtp.hostinger.com
MAIL_PORT=465
MAIL_USER=your@email.com
MAIL_PASSWORD=CHANGE_ME_mail_password
MAIL_FROM=Finders Keepers LB <noreply@finderskeeperslb.com>
